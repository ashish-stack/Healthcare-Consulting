server:
  port: 8400
  servlet:
    context-path: /user
spring:
  application:
    name: UserMs
  cloud:
    config:
      import-check:
        enabled: false
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: root
    url: jdbc:mysql://localhost:3306/user
    username: root
  jpa:
    generate-ddl: true
